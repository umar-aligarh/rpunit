const mongoose = require("mongoose");

const metaSchema = new mongoose.Schema({

    _id: {
        type: String,
        required: true
    },
    calltoServerCount:Number,
})

module.exports = metaSchema